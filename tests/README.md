# Run tests

To run the tests:

```bash
# go to your app folder
cd lai_youtubedownloader

# go to tests folder
cd tests

# install testing deps
pip install -r requirements.txt

# run tests
pytest .
```
